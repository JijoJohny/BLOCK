{
    "name": "aptolize-staking",
    "version": "1.0.0",
    "description": "A project for staking stablecoins using Aptolize",
    "scripts": {
      "start": "node src/index.js",
      "test": "hardhat test",
      "deploy": "hardhat run scripts/deploy.js --network rinkeby"
    },
    "devDependencies": {
      "dotenv": "^8.2.0",
      "ethers": "^5.0.0",
      "hardhat": "^2.0.0",
      "@openzeppelin/contracts": "^4.0.0"
    }
  }
  